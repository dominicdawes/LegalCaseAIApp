# Web service dependencies (Render web instance — keep lean)

# Core web
fastapi==0.111.0
uvicorn==0.30.1
websockets==12.0

# Celery / Redis (task dispatch only — no task execution here)
celery==5.4.0
gevent==23.9.1
redis

# Utilities
python-dotenv
loguru
psutil
httpx
pybreaker
tenacity==8.5.0
json_repair
typing_extensions>=4.0
fuzzywuzzy==0.18.0
python-Levenshtein>=0.20.9

# PDF utils (used directly by app/main.py → utils/pdf_utils.py)
PyPDF2

# Database / storage
supabase==2.5.0
psycopg2-binary==2.9.9
asyncpg>=0.28.0
boto3
botocore

# Auth
google-auth
